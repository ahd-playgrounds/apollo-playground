overwrite: true
schema: "server/schema.graphql"
documents: null
generates:
  server/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
